<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Base.Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="botoes"> <!--botoes de 1 a 9 + sinais (+-*/)-->
        <item name="android:textColor">#ffffff</item>
        <item name="android:textSize">16sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">match_parent</item>
    </style>

    <style name="botoes_numeros" parent="@style/botoes"> <!--//botoes de 1 a 9-->
        <item name="android:background">@drawable/grad_numeros</item>
    </style>

    <style name="botoes_sinais" parent="@style/botoes"> <!--sinais (+-*/)-->
        <item name="android:background">@drawable/grad_sinais</item>
    </style>

</resources>
